// Vercel serverless function entry point.
// Vercel imports this file and uses the exported Express app as the handler
// for all /api/* requests (configured via vercel.json rewrites).
export { default } from '../server/index.js';
